{{ content() }}
<section class="page" data-parallax="scroll" data-image-src="img/metal.jpg">
    <div class="section-content">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">{{ page.title }}</h2>
            </div>
            <div class="section-body">
                <div class="page-content">
                    {{ page.content }}
                </div>
            </div>
        </div>
    </div>
</section>